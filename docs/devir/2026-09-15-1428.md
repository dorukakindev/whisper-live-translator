# Handoff - English public-release documentation

## Identity

- Repository: `D:\Whisper Live`
- Branch: `main`
- Origin: `https://github.com/dorukakindev/whisper-live.git`
- Starting commit: `fbbc4ae54510646d32eec7d748f2ab2d5d8f0c9d`
- Completed public-documentation commit: `2f6861578738dbbebe23d2ff15814db00028e259`

## Work completed

- Added an English public-facing `README.md` describing the product, Windows requirements, setup, configuration, privacy boundaries, development checks, packaging, limitations, security, and licensing.
- Added the MIT `LICENSE`, `SECURITY.md`, `CONTRIBUTING.md`, `THIRD_PARTY_NOTICES.md`, and `PUBLIC_RELEASE_CHECKLIST.md`.
- Added English installation, usage, privacy, troubleshooting, and architecture guides under `docs/`.
- Added GitHub bug-report and pull-request templates that prohibit secrets and private conversation data and require evidence/validation boundaries.
- Translated `.env.example` comments to English, clarified localhost security, expanded `.gitignore` for `.env.*`, temporary output, and the local quick-phrase artifact, and updated package metadata.
- Added `npm run audit:history`. The scanner checks every reachable revision for common credential formats, redacts values, and prints only type, short SHA-256 fingerprint, commit, path, and line.

## Evidence and public-release blocker

- The current source tree passes the existing source security scanner.
- The new full-history scanner checked 62 reachable revisions and correctly returned exit code 1.
- It found nine unique path/fingerprint combinations: three OpenAI-compatible key candidates and one Google API key candidate in `archive/translator_gui.py`, plus one Hugging Face token candidate duplicated across five files under the historical `archive/__pycache__yedek/` tree.
- No credential value was copied into documentation or this handoff.
- The repository must remain private until the owner confirms revocation/rotation and explicitly approves a coordinated history rewrite. Deleting files in a new commit does not remove earlier Git objects; no force push or visibility change was performed.

## Usage

- New users start from `README.md`, then follow `docs/INSTALLATION.md` and `docs/USAGE.md`.
- Maintainers run `npm run scan` for the current source tree and `npm run audit:history` for all reachable history.
- Before changing visibility, complete every unchecked item in `PUBLIC_RELEASE_CHECKLIST.md`.
- Security reports should use GitHub private vulnerability reporting when enabled and must never include credentials or private recordings/transcripts.

## Validation

Passed:

- `git diff --cached --check`
- `node --check scripts/audit-git-history.js`
- `JSON.parse(package.json)`
- custom local Markdown-link and invalid/control-character validation
- `npm run scan` -> current source tree clean
- fetch comparison before work -> local `main` and `origin/main` were equal

Expected blocking result:

- `npm run audit:history` -> exit 1, nine redacted history candidates across 62 revisions

Not run because application behavior was not changed:

- Python backend regression suites
- Electron/browser tests
- real microphone/system-audio or provider calls
- `npm run build`
- clean-machine installation test

## Dependencies, configuration, and data

- No dependency versions or application data schemas changed.
- `package.json` gained the `audit:history` script and public metadata.
- `.env.example` contains placeholders only; no local `.env` value was read or committed.
- `.gitignore` now ignores `.env.*` except `.env.example`, `_quick_phrases.json`, and `*.tmp`.
- The existing local `_quick_phrases.json` is now ignored and was not committed.

## Remaining work

1. Revoke or verify every reported historical credential candidate.
2. Obtain explicit owner approval before any history rewrite or force push.
3. Rewrite affected branches/tags, ask collaborators to reclone, and rerun both scanners until history scanning exits 0.
4. Review GitHub issue/release/tag content and repository settings, enable private vulnerability reporting/secret scanning, and test a clean Windows installation.
5. Decide whether Turkish internal audit and handoff archives should remain visible in a future public repository.
6. Review exact dependency/model licenses from the final distribution set before publishing binaries.

At the time this note was created, its own documentation commit and push were still pending. The application/public-documentation commit above was local and would only be described as published after remote SHA verification.
