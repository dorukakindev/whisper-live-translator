# Devir notu — genel kod denetimi ve konsolide bug raporu

## Kimlik

- Repo: `D:\Whisper Live`
- Dal: `main`
- Origin: `https://github.com/dorukakindev/whisper-live.git`
- Kod commit'i: `b5e7910` (değişiklik yok; bu çalışma yalnız denetim + dokümantasyon)

## Yapılan iş

- Kullanıcının isteği üzerine tüm uygulama (backend, frontend, Electron, yardımcı betikler, build) statik olarak denetlendi; kodda değişiklik yapılmadı.
- Önceki iki rapor (`BUG_TARAMASI_TAM_RAPOR.md`, `DERIN_BUG_TARAMASI_RAPORU.md`) tek tek güncel koda karşı doğrulandı ve **`GENEL_DENETIM_RAPORU_2026-09-15.md`** dosyasında konsolide edildi.

## Bulgular (özet)

- Açık bug: 7 adet — A-01 Durdur'da son konuşmanın atılması (yüksek); A-02 `isOwnedWhisperBackend` 8.3 kısa yol hatası; A-03 backend çeviri + `autoTranslateEnabled` çift AI çevirisi; A-04 ilk GPU yüklemesinde OOM→sessiz CPU; A-05 Ctrl-PTT `keepalive` eksik; A-06 mic yolunda `conversation_turns` kilitsiz append; A-07 CSP yok.
- Bilinen sınırlama: L-01 telaffuz sözlüğü tam-eşleşme (tasarım kararı).
- Geliştirme önerileri: G-01..G-09 (raporda).
- Eski 30 maddenin 24'ü tamamen düzeltilmiş doğrulandı; 3'ü kısmen (BUG-06/12/17/24); 2'si açık (BUG-18→A-01, BUG-29→A-02); 1'i tasarım kararı (BUG-30→L-01).

## Doğrulamalar

- `python -m py_compile buyedektir.py` — geçti.
- `python -m pyflakes buyedektir.py` — temiz.
- `python test_smoke.py` — geçti (`TUM TESTLER GECTI`).
- `node --check` — tüm `static/*.js`, `main.js`, `main_helpers.js`, `preload.js`, `build.js` geçti; inline scriptler Jinja placeholder'ı literal ile değiştirilerek doğrulandı.
- `git status` — kod değişikliği yok.

## Bilinen sınırlar / sonraki adımlar

- A-01 ve A-04 gerçek donanımda yeniden üretilmedi; statik kanıtla raporlandı.
- Rapor öncelik sırası: A-01 → A-02 → A-04 → A-03 → A-05 → kalanlar.
- Kullanıcı düzeltme istemedi; uygulama koduna dokunulmadı.
