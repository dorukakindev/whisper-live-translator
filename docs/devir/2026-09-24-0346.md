# Devir Notu — 2026-09-24 03:46 (Europe/Istanbul)

## Kapsam ve Git durumu

- Repo: `dorukakindev/whisper-live-translator`; dal: `main`.
- Başlangıç kod commit'i: `a3e59ecb30305a16e9525bc91a78c86381489d1c`.
- Bitiş kod commit'i: `74103e7124697cce3c677a3a01cf3f895af2178f`.
- GitHub'da zaten birleşmiş PR #10–#12, temiz yerel çalışma ağacına `git merge --ff-only origin/main` ile taşındı. Uzakta `master` dalı yok; eski yerel `master` ayrı bir geçmişte olduğu için değiştirilmedi.
- Bu turda kod değiştirilmedi. Bu not ve kök devir indeksi ayrı belge commit'i kapsamındadır.

## İncelenen değişiklikler ve kanıt

- PR #10: `buyedektir.py` capture el sıkışmasıyla zaten iletilen hatanın ikinci socket toast'unu engelliyor; mikrofon açılış hatasını meşguliyet mesajından ayırıyor. `static/i18n.js` emoji önekli dinamik düğme etiketlerini çeviriyor. İlgili capture ve mic regresyonları geçiyor.
- PR #11: `.github/workflows/test-gate.yml` backend/frontend testlerini gerçek CI kapısına alıyor; `scripts/check-inline-scripts.js` Jinja şablonundaki inline JavaScript'i denetliyor. Son GitHub commit'inde `backend`, `frontend`, `source-safety` tamamlandı ve başarılı.
- PR #12: `tests/test_answer_harness.py` cevap önerisinin paralellik, kısmi socket yayını, tek çağrı hatası, seçenek sözleşmesi ve timeout akışlarını sahte sağlayıcıyla ölçüyor. Gerçek ücretli sağlayıcı çağrısı yapmıyor.
- Kritik OAuth, gizli anahtar, IPC veya yeni dosya/process yetki yolu değişikliği yok; CI izinleri `contents: read`. `git diff --check` temizdi.

## Yerel hedefli kontroller

- `.venv/Scripts/python.exe tests/test_answer_harness.py` → 6/6 OK.
- `.venv/Scripts/python.exe tests/test_capture_lifecycle.py` → 15/15 OK.
- `.venv/Scripts/python.exe tests/test_concurrency_audit.py` → 22/22 OK.
- `node scripts/check-inline-scripts.js` → 3 blok temiz.
- Tam test paketi yerelde yeniden çalıştırılmadı; son GitHub CI kapısının yeşil sonucu genel regresyon kanıtı olarak kullanıldı. Windows Electron fixture'ı önceki turdaki test-çıktı kanalı `EPIPE` sorunu nedeniyle tekrar başlatılmadı. Gerçek mikrofon, oyun penceresi, ücretli sağlayıcı ve EXE build denenmedi.

## Bağımlılık, kullanım ve kalan sınır

- Yeni bağımlılık, ayar veya şema değişikliği yok. Mevcut kullanıcı verisi ve `.env` okunmadı/değiştirilmedi.
- Yeni özelliklerin kullanımı önceki PR #10–#12 devir notlarında; bu tur yalnız eşitleme ve güvenlik/kanıt kontrolüdür.
- Kaynak `main` güncel olsa da daha önce üretilmiş `dist/` paketi kendiliğinden yenilenmez. Gerçek Windows ses aygıtı ve sağlayıcı davranışı ayrıca doğrulanmalıdır.
