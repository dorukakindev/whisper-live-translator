# Devir — Çeviri işçisi tutarlılığı

- Repo: [dorukakindev/whisper-live](https://github.com/dorukakindev/whisper-live), dal: `main`.
- Tarih: 2026-09-14 03:09, Europe/Istanbul.
- Başlangıç kod/depo commit'i: `b30b4a8079844018cfab624c280da3b0835b0ab9`.
- Bitiş kod commit'i: `1ede9ede72fe34be2cda14cdccfb7dcacdb5e49b`.

## Dosyalar, hatalar ve doğrulama

- `buyedektir.py`: sağlayıcı dönüşünden sonra oturum ve sıfırlama nesli yaşam döngüsü kilidi altında kontrol edilir. Önceden gecikme ölçümü bu kontrolden önce kaydediliyordu; eski iş yeni oturumun ölçümlerini kirletebiliyordu. Yeni test hem oturum hem nesil değişimini sağlayıcı çalışırken tetikleyip ölçüm, sonuç ve dosya kaydının eklenmediğini doğrular.
- `buyedektir.py`: yalnız dolu metin sonuçları kabul edilir, baş/son boşlukları temizlenir. Önceden boşluk metni veya dolu sözlük gibi truthy sağlayıcı çıktıları başarılı sayılabiliyordu. Boşluk, None, sözlük ve sayı senaryoları artık failed durumuyla sonuçlanır.
- `buyedektir.py`: belleğe kaydedilen sonuç ile sonrasındaki bildirim/dosya adımı ayrılır. Önceden sonraki hata genel exception yolundan done durumunu failed yapıyordu. Sentetik socket hatası testi metnin ve done durumunun korunduğunu, kilidin serbest bırakıldığını doğrular. Hata günlüğü ve sağlık hata kaydı korunur; başarısız bildirime otomatik tekrar eklenmedi.
- `test_translation_worker.py`: model veya ağ yüklemeden AST ile gerçek işçi metotlarını çalıştıran dört unittest testi eklendi. Normal başarı, oturum/nesil geçişi, bildirim hatası ve geçersiz sağlayıcı yanıtlarını kapsar.
- `DEVIR-NOTU.md`: güncel not bağlantısı değişti; önceki bağlantı korundu.
- `docs/devir/2026-09-14-0309.md`: bu çalışmanın bağımsız devir kaydı.

## Kullanım ve uyumluluk

Düzeltmeler canlı otomatik çeviride uygulanır; kullanıcı ayarı gerektirmez. Backend yeniden başlatılmalıdır. Yeni ekran veya bağımsız ürün özelliği eklenmedi. Yeni bağımlılık/kurulum gereksinimi, ayar veya şema değişikliği yok. Mevcut translation_status alanı kullanılır. Yeni test `.venv/Scripts/python.exe test_translation_worker.py` ile bağımsız çalıştırılabilir.

## Testler

| Komut | Sonuç |
|---|---|
| `.venv/Scripts/python.exe test_translation_worker.py` | 4 test geçti; alt senaryolar dahil. |
| `.venv/Scripts/python.exe -m py_compile buyedektir.py test_translation_worker.py` | Başarılı. |
| `.venv/Scripts/python.exe -m pyflakes buyedektir.py test_translation_worker.py` | Temiz. |
| `.venv/Scripts/python.exe test_live_audio.py` | Geçti. |
| `.venv/Scripts/python.exe test_smoke.py` | Son çalıştırma çıkış 1: yalnız test_speaker_reset_invalidates_inflight_result, Torch c10.dll / WinError 1114. Diğer ana kontroller geçti; tam paket başarılı değildir. |
| `npm run scan` | Kaynak taraması temiz. |
| `git diff --check` ve `git diff --cached --check` | Boşluk hatası yok; Git LF/CRLF uyarısı verdi. |

İlk smoke denemesinde tamamlanmış kayıtları genel olarak koruyan yaklaşım iki mevcut yeniden deneme kontrolünü bozdu. Koruma yalnız sonucu kaydetmiş mevcut işçinin hata yoluna daraltıldı; ikinci çalıştırmada bu iki hata yoktu. Test beklentileri değiştirilmedi.

Çalıştırılmayanlar: arşiv paketleri (smoke içinden DLL hatası nedeniyle başlamadı), frontend/Electron testleri (ön yüz değişmedi), canlı HTTP, gerçek mikrofon, ücretli sağlayıcı, TTS, GPU/diarization ve portatif build. Sentetik testler gerçek sağlayıcı doğruluğu veya ses kalitesini kanıtlamaz.

## Bilinen sorunlar ve sonraki adımlar

- Önceki devirdeki Torch DLL engeli sürüyor; bu çalışma bağımlılık değiştirmedi. Native yükleme sorunu giderilince tam smoke ve arşiv paketleri yeniden çalıştırılmalı.
- Bildirim hatasında sonuç bellekte korunur ancak canlı istemciye otomatik yeniden gönderim yoktur; geçmiş yeniden yüklendiğinde kayıt okunabilir. Teslimat tekrarının ayrıca tasarlanması gerekir.
- Gerçek uzun görüşme, HTTP açılışı ve sağlayıcı uçtan uca doğrulaması önceki devirden kalan işlerdir.

## Commit / push

Kod yukarıdaki bitiş commit'ine alındı. Bu not ve indeks ayrı belge commit'ine alınarak origin/main dalına push edilecektir. Bu paragraf notun hazırlanma anını kaydeder; henüz push başarısı iddiası değildir. Son yanıtta uzak HEAD doğrulaması ve gönderilen son belge commit kimliği verilir. Yeni test dışında kaynak eklenmedi; ortam, özel log, anahtar ve kişisel veri bu nota dahil edilmedi.
