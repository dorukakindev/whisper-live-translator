# Devir — 2026-09-22 07:57 (gece sağlamlaştırma P1–P13)

- Repo: `dorukakindev/whisper-live-translator`, dal: `devin/1790050008-overnight-hardening` (base `95f9b93`).
- Başlangıç kod commit'i: `95f9b93` → bitiş kod commit'i: `77e450e` (code commits: `fbddadf` P1–P4, `f2d6776` P5–P10, `77e450e` temizlik).
- Kapsam: yakalama → transkript → çeviri → cevap önerisi → Türkçe okunuş → overlay zincirinin 13 paketlik gece denetimi. Denetim raporu: `docs/overnight-hardening-2026-09-22.md`.

## Doğrulanmış hatalar ve düzeltmeler

1. **`ptt_mic_result` sahiplik kontrolü yoktu** (`templates/index.html`): olay tüm soket istemcilerine yayılıyor; handler `recording_id` karşılaştırmadan satır + uyarı ekliyordu. İkinci localhost istemcisinin Alt-PTT sonucu ana pencereye sızıyordu. Düzeltme: `data.recording_id !== altPttRecordingId` → erken çık. Test: Electron `P1a foreign ptt_mic_result ignored` (önce FAIL: 1 satır + 2 uyarı; sonra PASS).
2. **`transcription_translation_status` `pending` emit'i `revision` taşımıyordu** (`buyedektir.py` ~4434): frontend `(id, revision)` ile eşliyor; sözleşme tutarsızdı → tüm status emit'leri artık `revision` içeriyor.
3. **`buyedektir.log`a ham konuşma metni yazılıyordu** (3 satır: transcribe diag `repr(full_text[:60])`, halüsinasyon filtresi `full_text[:80]`, `Mic transcription: {full_text}`): artık yalnız karakter uzunluğu loglanıyor (`-> 42chr` vb.). Gizlilik: düz-metin log, konuşma içeriğini `transcriptions.txt` dışına ikinci kopya olarak taşıyordu.

## Yeni kalıcı testler / harness

- `tests/test_pronunciation_fixture.py` (9 test): 17 `PRONUNCIATION_GUIDES` dilinde temsili örnekler + sabitlenmiş çıktılar; charset invariantı; ja-tire kuralı; bozuk/boş model çıktısı; dil değişimi. Otomatik test telaffuz kalitesini değil sözleşme regresyonunu kanıtlar.
- `test_overlay_stale.js` (Electron, 8 kontrol): bayat revision, yabancı id/instance, mic/ptt kaynak filtresi, clear+geçikmiş çeviri dirilmemesi, instance geçişi.
- `tests/test_soak.py` (`SOAK_SECONDS`): sentetik akış + çeviri stub'ı + model-lock çekişmesi + PTT + start/stop döngüleri; RSS/thread/kuyruk/emit örneklemesi.
- `test_capture_lifecycle.js` +5 kontrol (P1a, P4, P6a–d, P8) → 21/21.
- `tests/test_capture_lifecycle.py` +7 test (PTT tampon tek-boşaltım, boş final, bayat partial, kuyruk dolması+lagging, socket token reddi, API token/JSON kontratı, healthz sızıntısı) → 15/15.

## Değişiklik gerekmeyen paketler (kanıtla)

- P2/P3/P4: sahiplik/sequence/revision guard'ları sağlam; eksik invariantlar testle kilitlendi.
- P6: DOM budama (100) + `transcriptionTexts` senkronu, dil/tema/arama çalışıyor; scroll-follow kod incelemesiyle doğru.
- P7: overlay stale-veri korumaları doğru; Windows oyun penceresi davranışı manuel doğrulamada.
- P8: onaylanmış açık yok; `GET /` üzerinden app_token localhost güven modeli gereği kabul edilmiş (HOST=127.0.0.1 korunmalı).
- P9: sınırsız retry yok; alert cap 3, lagging 8 sn rate-limit, profil-kayıt hatası uyarı+tmp temizliği doğru.
- P11: kilitli bağımlılıklar kurulu, taramalar temiz; wine64 yok → EXE build çalıştırılmadı.
- P12: İngilizce dokümanlar kodla tutarlı.

## Test sonuçları

- `test_smoke.py` → TUM TESTLER GECTI (archive + concurrency 21 + capture 15 + pronunciation 9 dahil).
- Electron/Xvfb: `test_capture_lifecycle.js` 21/21, `test_overlay_stale.js` 8/8, `test_cockpit_browser.js` PASS.
- Soak 30 dk: 1599 final + 26898 partial emit, 40 temiz start/stop döngüsü, RSS +6.3 MB, thread +1, kuyruk max 1, 0 hata — sızıntı örüntüsü yok. Sahte model/akış gerçek Whisper belleğini kanıtlamaz.
- `py_compile`, `pyflakes`, `node --check` (main.js + 2 inline blok), `npm run scan`, `npm run audit:history` (80 revizyon), `git diff --check` → hepsi temiz.
- Çalıştırılmayan: `npm run build` (wine64 yok — ortam engeli); `test_cevap_onerisi.py` (ücretli API, bilinçli atlandı).

## Kalan riskler / Windows manuel doğrulama

Raporun "Windows manual-verification" bölümü: EXE build, gerçek WASAPI + cihaz kopması, Game Mode gerçek oyun/DPI/çoklu monitör, Alt-PTT gerçek mikrofon, pyannote. Ayrıntılar: `docs/overnight-hardening-2026-09-22.md`.

## Notlar

- `.agents/` (skill önerisi) çalışma ağacında izlenmiyor; commit'e dahil edilmedi.
