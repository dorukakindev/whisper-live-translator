# Devir Notu — 2026-09-25 23:59

## Özet

Aynı gün 03:45'teki ilk A→Z taramanın üzerine ikinci bağımsız tam tur yapıldı
(kod tabanı aynı: HEAD `eacfcdb`, ara commit'ler yalnız doküman). Kodda
değişiklik yapılmadı; bulgular `docs/bug-raporu-2026-09-25-ek.md` dosyasına
yazıldı.

## Bağlantılar

- Repo: `dorukakindev/whisper-live-translator`, dal `main`,
  başlangıç/bitiş kod commit'i: `eacfcdb` (kod değişmedi).
- Rapor: [docs/bug-raporu-2026-09-25-ek.md](../bug-raporu-2026-09-25-ek.md)
- Önceki tur raporu: [docs/bug-raporu-2026-09-25.md](../bug-raporu-2026-09-25.md)

## Bu turda yapılanlar

- `buyedektir.py` kalan kritik blokları yeniden okundu: capture döngüsü
  (3880-4180), `_transcribe_audio` commit yolu (4242-4530), `_transcribe_partial`
  (4531-4630), `MicRecorder` (2329-2526), `snapshot_request`/`translate`
  (1941-2015), `update_settings`/`get_capture_profile` (3413-3453),
  `get_audio_devices` (3473-3529), `/api/status` + `healthz` + `audio_test`
  (5483-5562), `deepl_config` + `translation_settings` (4906-4994), auth/CSP
  katmanı (95-190).
- `templates/index.html`: `resyncAfterReconnect`, capture_started/stopped
  handler'ları, sessizlik slider akışı, `persistRuntimeSettings` doğrulandı.
- `audio_diagnostics.py` tamamen; `test_live_audio.py` çalıştırılarak W-4
  ampirik teyit edildi (`AttributeError: _capture_handshake`).
- Kökteki tüm `test_*.py`/`test_*.js` dosyalarının kapı-bağlantısı envanteri
  çıkarıldı; çalışabilenler koşuldu (hepsi geçti; 4 dosya Electron harness'i).

## Yeni doğrulanmış bulgular

1. **W-5 (düşük)** — `/api/status` `session_id`'yi düşürüyor; snapshot üretir
   (`buyedektir.py:2930`), route kopyalamaz (5501-5518). Sonuç:
   `index.html:3741`'deki oturum evlat-edinmesi ölü kod; sayfa yenilemesi +
   gecikmiş `capture_stopped` yarışında UI senkron kaybedebilir. Tek satırlık
   düzeltme önerisi raporda.
2. **W-6 (düşük, kontrat/UX)** — sessizlik slider'ı 0.2–5s sunar
   (`index.html:2309`) ama adaptif mod `[0.65,2.0]`'a kıstırır
   (`audio_diagnostics.py:37,56`) ve oyun modu 0.9s'e sabitler
   (`buyedektir.py:3448-3452`); etkin değer UI'ya hiç yansıtılmaz. Clamp'in
   kendisi kasıtlı güvenlik zarfı — kusur sessiz sapmada.

## Hâlâ açık (yeniden doğrulandı)

- **W-1 (kritik)** — `/api/translation_settings` boş `apiKey` kayıtlı anahtarı
  siliyor; frontend nesnesi `apiKey` alanını her zaman taşıyor
  (`index.html:2646`, POST 3181/3224; backend 4985-4992).
- `/api/deepl_config` AI dalı aynı kökün kardeş yüzeyi: `api_key` alansız
  çağrı `configure_translation(provider, None)` → anahtarı siler + provider'ı
  koşulsuz değiştirir (`buyedektir.py:4924-4925`).
- **W-4** test borcu — ampirik `AttributeError`; `test_smoke.py` bu dosyayı
  koşmuyor (ölü test). Ayrıca kök testlerinin çoğu hiçbir kapıya bağlı değil.

## Test / doğrulama sonuçları

- `test_live_audio.py` → ❌ `AttributeError` (W-4 teyidi; ürün kodu sağlam).
- `test_audio_diagnostics.py` ✅ 5/5, `test_live_enhancements.py` ✅ 16/16,
  `test_translation_worker.py` ✅ 4/4.
- Node ile koşabilen 13 `test_*.js` ✅ (lifecycle, cockpit, frontend
  regresyonları, game mode/overlay dahil).
- 4 JS dosyası Electron gerektirir; düz `node` ile `TypeError` beklenir —
  bug değil, yanlış koşucu.

## Değişen dosyalar

- `docs/bug-raporu-2026-09-25-ek.md` (yeni), bu not, `DEVIR-NOTU.md` indeks satırı.
- Uygulama kodu: **dokunulmadı**. `nul` adlı yabancı untracked dosya bırakıldı.

## Sonraki adımlar (öneri)

1. W-1 + `deepl_config` kardeş yüzeyini tek yamayla kapat: boş/`None` `apiKey`
   alanını "değiştirme" yerine "yoksay" ya da açık `null`=sil semantiğine ayır.
2. `/api/status`'a `session_id` ekle (W-5).
3. W-6 için rapordaki (a)/(b)/(c) seçeneklerinden biri.
4. `test_live_audio.py` fixture'ını hizala ya da `tests/` altına taşı + kapıya bağla.
