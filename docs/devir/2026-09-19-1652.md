# Devir Notu — 2026-09-19 16:52 (Europe/Istanbul)

**İş:** Kullanıcı talebiyle tam test turu + derin bug avı; bulgular tek birleşik raporda toplandı ve repoya commit'lendi. **Uygulama kodunda bilinçli değişiklik yok** — yalnız rapor ve bu not eklendi.

## Commit'ler

- Başlangıç HEAD: `358ca1e` (docs: record Claude provider handoff)
- Rapor commit'i: `77598d0` — `BUG_BULGULARI_TUM_2026-09-19.md` (kök)
- Bu devir commit'i: (bu not)

## Değişen dosyalar

- `BUG_BULGULARI_TUM_2026-09-19.md` (yeni, kök) — birleşik rapor: test matrisi, yeni bulgular N-1..N-14, 2026-09-15 denetiminin 22 açık bulgusunun yeniden doğrulama tablosu, sağlam alanlar, öncelik sırası, sınırlar.
- `docs/devir/2026-09-19-1652.md` (bu dosya)
- `DEVIR-NOTU.md` (indeks güncellemesi)
- `DERIN_TEST_VE_BUG_RAPORU_2026-09-19.md` — aynı gün erken turdaki geçici rapor; içeriği birleşik rapora taşındı, dosya commit'lenmedi ve silindi.

## Çalıştırılan testler

- `python test_smoke.py` → geçti
- `tests/archive/` 4 Python süiti → geçti
- `tests/` 12 Node + 3 Electron testi (xvfb) → geçti; `test_game_overlay_native.js` 4 koşuda 1 kez flaky (N-7)
- `npm run scan` → temiz
- `py_compile`/`pyflakes` ve `node --check` → temiz
- `test_cevap_onerisi.py` → ücretli API gerektirir, atlandı
- Flask `test_client` ile canlı uçlar: yetkisiz istek 401'leri, JSON fuzz, eşzamanlılık, N-1/N-8/N-9 canlı yeniden-üretim

## Öne çıkan bulgular (ayrıntı raporda)

- **N-1 🔴** `/api/ai_chat` (buyedektir.py:6202) yalnız OpenAI anahtarına bakar → Claude-only kullanıcıda özet/soru kapalı.
- **N-2 🔴** 4314'teki sağlayıcı seti `'anthropic'` içermiyor → Claude canlı çeviri hiç kuyruğa girmiyor; ayrıca `ANTHROPIC_API_KEY` env'i `translation_api_keys['anthropic']`'i hiç tohumlamıyor.
- **N-9 🔴** `/api/translation_settings` (4827) `apiKey` alan yoksa/boşsa `translation_api_keys[provider]=None` → saklı çeviri anahtarı siliniyor (canlı ispatlandı); DeepL dalında `'apiKey' in data` koruması var, AI dallarında yok.
- **N-3 🟠** 5094'te aynı eksik set → Alt-PTT'de Claude ile okunuş (romanized) üretilmez; kart okunamaz yerli yazı gösterir.
- **N-4 🟠** index.html açılış zinciri backend yapılandırmasını eziyor: koşulsuz `/api/ai_translation_model` POST'u, eski localStorage provider'ın 'anthropic'e zorlanması, her açılışta anahtar re-POST + canlı `test_connection`.
- **N-8 🟡** `generate_ai_response` string-olmayan `text`'te 500 (isinstance eksik) — canlı ispatlandı.
- Diğerleri: N-5 (PTT "recording:true" ama capture ölü olabilir), N-6 (transkribe.py model-swap yarışı), N-7 (flaky test), N-11 (banner mojibake), N-12 (overlay timeout yok), N-13 (anthropic doğrulama önbelleği yok), N-14 (küçük girdiler).
- 2026-09-15 raporunun 22 bulgusu yeniden doğrulandı: B-01 (pyannote 4.x `itertracks`) hâlâ açık, C-01 genelleşti, B-08 kapandı, diğerleri tabloda.

## Çalıştırılamayanlar / sınırlar

- Windows'a özgü ses yolları (pyaudiowpatch/WASAPI) Linux'ta stub; gerçek uçtan-uca ses akışı Windows'ta doğrulanmalı.
- Gerçek sağlayıcı çağrıları anahtar/ücret nedeniyle stub.
- `/api/devices` Linux'ta 500 — platform sınırlaması.

## Ortam kurulumu (bu oturumda yapılanlar — repoya dokunulmadı)

- `~/repos/whisper-live-translator/.venv` (python3.10) kuruldu; `pyaudiowpatch` Linux stub'ı yalnız venv site-packages'ta.
- `apt` ile `python3.10-tk`, CPU `torch`; Node v20.19.0 `/usr/local`'e; Electron testleri `DISPLAY=:0` xvfb altında.
- `test_followup_frontend.js`'in Windows venv yolu için yerel symlink shim kullanıldı (repo değişmedi).

## Sonraki adımlar (öneri)

1. N-2+N-9+C-01'i tek PR'de topla (anthropic set'e ekleme + `apiKey` koruması + env tohumlama).
2. N-1 kapı düzeltmesi; N-3 aynı set düzeltmesi.
3. N-4 frontend açılış POST'larını koşullandırma.
4. B-01 pyannote 4.x uyumu ayrı iş.
