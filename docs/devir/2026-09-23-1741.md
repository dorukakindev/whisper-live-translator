# Devir Notu — 2026-09-23 17:41 (Europe/Istanbul)

## Özet

Cevap-önerisi zinciri için stub'lı deterministik kalite/latency harness'i: `tests/test_answer_harness.py` (6 test, ~2.5s). Provider çağrıları `answer_question` seviyesinde sahtelenir; socket emit'leri toplayıcıyla izlenir; hiç rastgele sleep yok.

- Repo: `dorukakindev/whisper-live-translator`
- Dal: `devin/1790174436-answer-harness` → PR #12
- Başlangıç kod commit'i: `7f32293` (main, PR #10 merge sonrası)
- Bitiş kod commit'i: `6e71421`

## Değişen dosyalar

- `tests/test_answer_harness.py` (YENİ): 6 deterministik test — paralel latency sınırı (<0.75s, sıralı olsaydı ≥0.8s garanti), `ai_options_partial` tamamlanma sırası + request_id sözleşmesi, request_id'siz istekte emit yok ama HTTP döner, tek-çağrı hata toleransı, ≤2/çağrı dedup + gürültü ayıklama + romanize charset, takılan çağrının kısaltılmış `as_completed` timeout'uyla <1.5s'de elenmesi.

## Kapsam dışı (ayrı, opt-in)

Gerçek provider eval'i (latency yüzdelikleri + seçenek kalite puanı) mevcut `test_cevap_onerisi.py`'dir — `OPENAI_API_KEY`/`ANTHROPIC_API_KEY` gerekli, gerçek API kredisi harcar, CI'a girmez.

## Testler

- `python -m pytest tests/test_answer_harness.py` → 6/6 PASS, 3 ardışık koşu (2.45/2.50/2.58s)
- `pyflakes` temiz

## Bilinen sorunlar / sonraki adımlar

- PR #11 merge edilirse harness CI backend job'unda otomatik koşar (`pytest tests/` kapsamında).
- Testing agent'ın bloklamayan bulguları açık: emoji-only butonların uzun `title` tooltip'leri için EN sözlük girdisi eksik; cihaz kopması ~25 benzer toast basıyor (throttle kaba).
