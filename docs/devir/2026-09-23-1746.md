# Devir Notu — 2026-09-23 17:46 (Europe/Istanbul)

## Özet

GitHub Actions'ta gerçek test kapısı: `.github/workflows/test-gate.yml` — push/PR'da backend + frontend suite'leri ubuntu-latest'te koşar. Bundan önceki tek kontrol `source-safety` idi; "yeşil = testler geçti" yanılgısı bitti.

- Repo: `dorukakindev/whisper-live-translator`
- Dal: `devin/1790173814-ci-test-gate` → PR #11
- Başlangıç kod commit'i: `7f32293` (main)
- Bitiş kod commit'i: `81cdf2e`

## Değişen dosyalar

- `.github/workflows/test-gate.yml` (YENİ): iki job, `permissions: contents: read`, 30dk timeout.
  - **backend:** py3.12 (pip cache) → sed'le `pyaudiowpatch|torch|torchaudio|faster-whisper|pyannote.audio` satırları requirements'tan atılır → `pip install` slim set + `torch` (pytorch.org/whl/cpu) + `faster-whisper` → site-packages'a pyaudiowpatch stub → pyflakes + py_compile → `pytest tests/ -q` → `test_smoke.py`.
  - **frontend:** node22 (npm cache) → xvfb + Electron lib'leri → `.venv/Scripts/python.exe` symlink (test_followup_frontend.js'in sabit Windows yolu) → `npm ci` → `npm run scan` → `node --check` tüm js + `scripts/check-inline-scripts.js` → 10 plain-node testi → 4 Electron fixture'ı `ELECTRON_DISABLE_SANDBOX=1 xvfb-run` altında.
- `scripts/check-inline-scripts.js` (YENİ): `templates/*.html`'deki inline `<script>` bloklarını Jinja'dan arındırıp `vm.Script` ile derler — `node --check`'in kapsamadığı inline JS'i CI'a dahil eder.

## CI'da karşılaşılan ortam farkları (düzeltmeler)

1. `.venv/Scripts/python.exe` — blueprint'in Linux stub symlink'i CI'da yoktu → adım eklendi.
2. Electron chrome-sandbox SUID → `ELECTRON_DISABLE_SANDBOX=1` (fixture'lar güvenilmeyen içerik yüklemez).
3. `test_smoke.py`'nin `transkribe`/`speaker_reset` alt testleri GERÇEK `faster_whisper`/`torch` import'u ister → CPU-only torch + faster-whisper açıkça kuruldu (PyPI torch ~2.5GB CUDA yerine ~200MB).

## Kapsam dışı / bilinen sınırlar

- Linux'ta takılan Electron testleri CI dışı: `test_game_mode.js`, `test_main_helpers.js`, `test_game_overlay_native.js` (gerçek Windows pencere davranışı ister).
- `torchaudio`/`pyannote.audio` kurulmaz — hiçbir kalıcı test import etmez; requirements'taki tembel-import notu korunur.
- Sed-listesi drift riski workflow yorumunda dokümante.

## Testler

- PR #11 CI: **backend ✅ frontend ✅ source-safety ✅** (yeşil).
- Lokal: `pytest tests/` 111+838, `test_smoke.py` TUM TESTLER GECTI, 10 node testi, 4 Electron testi, `node --check` + inline-check temiz.

## Sonraki adımlar

- Merge sonrası `test_answer_harness.py` (PR #12) da kapıda koşar.
- İsteğe bağlı: Electron testlerini Windows runner'a taşımak (gerçek sandbox + oyun-overlay yolları).
