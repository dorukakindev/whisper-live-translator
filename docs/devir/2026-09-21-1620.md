# 2026-09-21 16:20 — Derin eş-zamanlılık denetimi ve doğrulanmış düzeltmeler

## Bağlam

- Repo: `dorukakindev/whisper-live-translator`
- Dal: `devin/1789993638-concurrency-audit` → hedef `main`
- Başlangıç commit'i: `3c91b45` (origin/main HEAD ile aynı)
- Bitiş kod commit'i: `35f78fd`
- Ayrıntılı rapor: `docs/concurrency-audit-2026-09-21.md`

## Ne yapıldı

Kullanıcı isteği: canlı-oturum durum makinesi ve tüm asenkron sınırlar için
derin denetim — deterministik yeniden-üretim + her doğrulanan hata için
düzeltme + kalıcı regresyon testi. Kaynak haritalama önceki oturumda
tamamlanmıştı; bu oturumda testler yazıldı, önce hata üreterek doğrulandı,
ardından dokuz odaklı düzeltme uygulandı.

Doğrulanan hatalar (hepsi düzeltildi — özet):

- `/api/ptt`: sırasız `active=true` sahiplik denetimini tamamen atlıyordu;
  reddedilen (409) start sahipliği yine de kaydedip eski sayfayı kalıcı
  kilitliyordu; `source` haritaları sınırsız büyüyordu.
- `_transcribe_audio`: `translate_executor.submit` reddi kaydı sonsuz
  `pending`'te bırakıyordu; `diarize_executor.submit` reddi commit edilmiş
  transkript için sahte 'error' emit ediyordu.
- `MicRecorder._record`: okuma istisnası sessizce öldürüyordu (UI bildirimi yok).
- `/api/ptt_mic`: iptal edilmiş `recording_id` ile start false-success
  dönüyordu; `result_generation` `mic_recorder.stop()` sonrası örnekleniyordu —
  join arasına giren Stop/Reset sonucun sızmasına izin veriyordu.
- `process_mic_audio`: başarı emit'i patlarsa ikinci/yanıltıcı hata terminali
  gidiyordu (≤1 terminal sözleşmesi bozuluyordu).
- `_append_transcript` dosya yazımı `_lifecycle_lock` altında (3 site) ve
  `save_profiles` `_profile_lock` altında çalışıyordu — yavaş diskte oturum
  geçişleri ve profil işleri bloklanıyordu.

## Testler

- `tests/test_concurrency_audit.py` (yeni, smoke'a bağlı): 20 test —
  13 hata üretimi + 17 durumlu komut matrisi + 20×25 property interleaving.
- `test_smoke.py`: TUM TESTLER GECTI (yeni paket dahil).
- 12 Electron'suz `test_*.js`: hepsi geçti.
- `py_compile` + `pyflakes` temiz; `node --check main.js` temiz;
  tüm inline `<script>` blokları ve `static/*.js` temiz; `npm run scan` temiz.
- Electron testleri xvfb ile: `test_ui_language.js`, `test_cockpit_browser.js`
  (eskiyen `io()` stub'ı `io({auth})` imzasına güncellendi),
  `test_game_overlay_native.js` — hepsi geçti.
- Manuel backend smoke: `GET /` 200, `/api/stats` 200 (token) / 403 (tokensuz).
- `npm run build` → ORTAM ENGELİ: win32 hedef `wine64`/rcedit ister; Ubuntu
  kutusunda wine yok, sudo yok. Kod sorunu değil.

## Bilinen sınırlar

- WASAPI/PyAudio donanımı simüle edildi (sahte akışlar); gerçek cihaz davranışı
  Windows'ta ayrıca doğrulanmalı.
- Alt-PTT uçtan uca tuş etkileşimi manuel doğrulama gerektirir.

## Blueprint notu

- Oturumda Devin ortam blueprint'inde `pyaudiowpatch` stub'ının
  `python3.10` yoluna yazıldığı bulundu; venv python3.12 kullanıyor. Zamanı
  bağımsız yol (`sysconfig`) kullanan güncelleme kullanıcıya öneri olarak
  gönderildi.
