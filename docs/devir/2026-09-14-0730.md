# Devir — Yeni oturum ve format sonrası rehber

- Repo: [dorukakindev/whisper-live](https://github.com/dorukakindev/whisper-live).
- Dal: `main`. Tarih: 2026-09-14 07:30, Europe/Istanbul.
- Başlangıç ve bitiş kod/depo referansı: `fd9c8cd3b8465c2495d80a7014f8a6f32ca0ac20`. Bu tur uygulama kodu değiştirilmedi; aynı referans geçerlidir. Bu referans önceki belge commit'idir, içerdiği son uygulama kodu commit'i `1ede9ede72fe34be2cda14cdccfb7dcacdb5e49b`.

## Amaç ve değişen dosyalar

Kullanıcı Codex'i sıfırlayacak ve bilgisayara format atacak. Konuşma geçmişine ve mevcut bilgisayardaki kurulu ortama ihtiyaç duymadan devam etmek için kalıcı talimat istedi.

- `AGENTS.md`: yeni oturumun okuma sırası; gerekli araç/paket/model kurulumu için kullanıcının verdiği yetki; mevcut değişiklikleri koruma ve her düzeltme grubundan sonra test/devir/commit/push yükümlülüğü eklendi.
- `BASLANGIC.md`: format öncesi özel yedek sınırları, temiz Windows kurulumu, GitHub erişimi, bağımlılıklar, yerel .env, native import kontrolleri, test/yayın sırası ve bilinen sorunlar yazıldı.
- `DEVIR-NOTU.md`: başlangıç rehberine ve bu nota bağlantı eklendi; önceki not bağlantıları korundu.
- `docs/devir/2026-09-14-0730.md`: bu çalışma kaydı.

## Hatalar ve özellikler

Uygulama hatası düzeltilmedi veya ürün özelliği eklenmedi. Bilgi kaybına karşı çalışma rehberi eklendi. Yeni Codex repo klasöründe önce AGENTS.md, sonra BASLANGIC.md ve güncel devir notunu okumalıdır. Kullanıcı yeni oturuma “AGENTS.md, BASLANGIC.md ve DEVIR-NOTU.md dosyalarını okuyup kaldığın yerden devam et” diyebilir.

## Doğrulamalar

- `git status --short`: başlangıçta temiz.
- `git rev-parse HEAD`: başlangıç referansı doğrulandı.
- `requirements.txt`, `package.json`, `.env.example`, `.gitignore` ve mevcut devir indeksi okundu; rehberdeki komutlar/ignore sınırları bunlara göre yazıldı. Gerçek .env okunmadı.
- `npm run scan`: kaynak taraması temiz.
- `git diff --check`: boşluk hatası yok.
- Yeni/değişen dört Markdown dosyasındaki yerel bağlantı hedefleri Python pathlib ile kontrol edildi; hepsi mevcut.
- Uygulama testleri, Electron, native import, temiz Windows kurulumu, canlı ses/HTTP/sağlayıcı ve build bu tur çalıştırılmadı; yalnız belgeler değişti. Yazılan kurulum komutları bu tur uygulanmadı.

## Bağımlılıklar, yarım işler ve yayın

Yeni bağımlılık, kurulum, ayar veya şema değişikliği yapılmadı. Rehber mevcut manifestleri kullanır; gelecekte sürüm/native uyumluluğunun yeniden doğrulanmasını ister. Önceki Torch c10.dll / WinError 1114 engeli ve gerçek uçtan uca test eksikleri sürüyor; çözülmüş sayılmadı.

Format veya özel yedekleme yapılmadı. GitHub dışında kalan sırlar, favoriler ve kişisel geçmiş kullanıcı tarafından korunmalıdır; bunların yedeği alındığı iddia edilmez. Sonraki adım yeni Windows'ta rehberle kurulum ve bilinen doğrulama eksiklerini tamamlamaktır.

Belge hazırlanırken dört belge değişikliği henüz commit/push edilmemişti; uygulama kodunda bekleyen değişiklik yoktu. Bu belge grubu tek belge commit'i olarak push edilecek. Son yanıtta uzak dal kimliğiyle doğrulanmış son SHA ve doğrudan not bağlantısı verilecek; bu paragraf hazırlanma anını kaydeder. Önceki devirler değiştirilmedi.
