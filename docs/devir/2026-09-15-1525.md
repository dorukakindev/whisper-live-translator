# Handoff - English product screenshots and demo video

## Identity

- Repository: `D:\Whisper Live`
- Branch: `main`
- Origin: `https://github.com/dorukakindev/whisper-live.git`
- Starting commit: `811970388222af92f77cb6c31ab2340e0ed2ace3`
- Completed media commit: `7df142c6cea2d679e3c1a2efe05790276e1f2c56`

## Work completed

- Added a prominent English product showcase to the top of `README.md`.
- Added a 1440 x 900 live-conversation screenshot, a 1440 x 900 reading-mode screenshot, a 1280 x 720 H.264 MP4, and an autoplaying GIF preview under `docs/media/`.
- Added `scripts/generate-public-demo.js` and the `npm run demo:media` command so maintainers can regenerate the media after UI changes.
- The generator loads the current `templates/index.html`, theme CSS, and production frontend modules in Electron/Chromium. It disables network/provider work and inserts only synthetic German/English sample content.
- The presentation labels are English without changing the shipping application's primarily Turkish interface. The README states this boundary next to the media.
- Removed the older Turkish game-overlay image from the README showcase so the public-facing introduction remains visually consistent in English.

## Media details

- `whisper-pro-conversation.png`: 122,417 bytes.
- `whisper-pro-reading-mode.png`: 88,728 bytes.
- `whisper-pro-demo.mp4`: 235,454 bytes; H.264, yuv420p, 1280 x 720, 7.4 seconds.
- `whisper-pro-demo.gif`: 1,254,251 bytes.
- The video is silent and presents the actual rendered conversation workspace followed by reading mode with a short crossfade.
- No real conversation, credential, microphone input, or provider response is present in any media file.

## Validation

Passed:

- `node --check scripts/generate-public-demo.js`
- `npm run demo:media`
- direct visual inspection of both full-resolution PNG files
- `ffprobe` verification of MP4 duration, dimensions, codec, and pixel format
- `git diff --check`
- `npm run scan` -> current source tree clean
- `.\node_modules\.bin\electron.cmd test_cockpit_browser.js` -> all real Chromium cockpit behavior and visual checks passed on the second run

Transient environment issue:

- The first cockpit run completed its assertions through the final narrow-layout phase, then Electron reported `Current display surface not available for capture` while saving a screenshot. A clean second process completed the entire suite. Electron also printed existing test-profile cache ACL and development CSP warnings; they did not fail the second run.

Not run because production application behavior and backend code did not change:

- Python compilation, pyflakes, and backend smoke suites
- real microphone/system-audio capture
- external AI or translation provider calls
- `npm run build`
- clean-machine installation test

## Dependencies, configuration, and data

- No dependency version, runtime setting, credential format, or application data schema changed.
- Regeneration uses the already installed Electron development dependency and requires `ffmpeg` on `PATH`. The generated media itself has no new runtime dependency.
- Temporary fixture and Electron profile files stay under ignored `artifacts/public-demo/`.
- The media generator performs no network request and reads no local `.env`, transcripts, recordings, or speaker profiles.

## Remaining work and publication boundary

- The full-history audit documented in the previous handoff still reports historical credential candidates. Keep the repository private until those candidates are revoked or verified and a separately approved history cleanup is complete.
- After any material UI change, rerun `npm run demo:media`, inspect the images, and commit refreshed media.
- A future public release may benefit from a real narrated screen recording, captions, and an English application locale; none of those are claimed here.

At the time this note was created, its own documentation commit and push were still pending. The media commit above was local and would only be described as published after remote SHA verification.
