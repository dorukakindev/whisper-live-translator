# Devir Notu — 2026-09-24 05:20 (Europe/Istanbul)

## Özet

AGENTS.md'ye "Windows birincil ortam (kullanıcı kuralı)" bölümü eklendi — kullanıcının kalıcı çalışma talimatının repo-içi sabitlenmesi.

- Repo: `dorukakindev/whisper-live-translator`
- Dal: `devin/1790216407-windows-first-rules` → PR
- Başlangıç commit'i: `8ec70dc` (main)
- Bitiş commit'i: `8d90c31`

## Değişen dosyalar

- `AGENTS.md` (+12 satır): `install.bat`/`start.bat` akışı (repo karşılığı: kök `başlat.bat` + `diger-baslaticilar/`), Windows yolları, PowerShell/cmd, dosya seçici, UTF-8/Türkçe karakter, GPU/CUDA kontrolü; scoped-test kuralı (tam paket gereksiz koşulmaz); Ubuntu Electron'un Windows arayüz/DPI/GPU/DRM/hesap doğrulaması yerine geçmediği notu; `main` üzerinden ayrı dal+PR, kendi PR'ı merge edilmez; Windows riskleri + doğrulanmamış sınırlar devir notuna yazılır; gerçek profil/anahtar/kişisel veri testte yok.
- `docs/devir/2026-09-24-0520.md` (bu not), `DEVIR-NOTU.md` indeks güncellemesi.

## Testler

Docs-only değişiklik — test koşulmadı (`git diff --check` temiz). Windows etkisi: yok (yalnız Markdown).

## Bilinen sınırlar

- `install.bat`/`start.bat` repo'da dosya olarak YOK — kullanıcı tarafındaki isimler; repo karşılığı `başlat.bat`/`diger-baslaticilar/`. Bölümde bu not düşüldü.
