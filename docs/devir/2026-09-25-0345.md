# Devir Notu — 2026-09-25 03:45 (Europe/Istanbul)

## Kapsam ve Git durumu

- Repo: `dorukakindev/whisper-live-translator`; dal: `main`; origin: `https://github.com/dorukakindev/whisper-live-translator.git`.
- Başlangıç kod commit'i: `8ec70dc0c9e3034e33c080b57592d748529b6176`.
- Bitiş kod commit'i: `8ec70dc0c9e3034e33c080b57592d748529b6176` — **bu turda uygulama kodu bilinçli olarak değiştirilmedi** (kullanıcı talimatı: "raporu sen kodu değiştirme").
- Değişen dosyalar (yalnız belge): `docs/bug-raporu-2026-09-25.md` (yeni), bu devir notu, `DEVIR-NOTU.md` indeks güncellemesi. Ayrıca `npm run build` ile `dist/Whisper-Pro-win32-x64/` yeniden üretildi (dist repoya commit'lenmez).

## Ne yapıldı

- A'dan Z'ye tam tur: `buyedektir.py` (~6529 satır), `templates/index.html` (~6411 satır inline JS dahil), `overlay.html`, `static/*.js` (7 dosya), `main.js`, `main_helpers.js`, `preload.js`, `build.js`, `transkribe.py` satır satır okundu.
- `BUG_BULGULARI_TUM_2026-09-19.md` (N-1..N-14 + A/B/C/D serisi) ve `overnight-hardening-2026-09-22.md` (P1-P13) bulguları HEAD'de tek tek yeniden doğrulandı: büyük çoğunluk düzeltilmiş (tam tablo yeni raporda §3).
- Flask `test_client` ile 27 sözleşme + 20 W-1 senaryosu canlı problandı (tümü geçti/üretildi; prob betikleri geçici `%TEMP%` dosyalarıydı, repoya eklenmedi).

## Doğrulanmış yeni bulgu

- **W-1 (🔴):** `/api/translation_settings` koruma yalnız `apiKey` alanının *yokluğuna* bakıyor (`buyedektir.py:4985`); frontend `translationSettings`'i `apiKey:''` ile **her sayfa açılışında/dil/sağlayıcı değişiminde** POST ettiği için (`index.html:2643-2649, 3103-3113, 3181, 3224`) boş alan `configure_translation(provider,'')` → `translation_api_keys[provider]=None` yapıp kayıtlı çeviri anahtarını siliyor. En çok `ANTHROPIC_TRANSLATION_API_KEY` env kullanıcısını vuruyor (N-2 düzeltmesinin tohumladığı anahtar ilk açılışta siliniyor). DeepL dalı ve `deepl_config` AI dalında da aynı kökten ikincil yüzeyler var. Canlı kanıt + tam zincir raporda.
- **W-4 (test borcu):** `test_live_audio.py` (kökteki eski script) `_capture_handshake` alanını bilmeyen `SimpleNamespace` kullanıyor → `AttributeError`; üretim kodu sağlam (2577/3650). Kod yasağı nedeniyle fixture'a dokunulmadı.
- Küçük artıklar: stop'ta >10 sn'lik kuyruk hâlâ düşer (A-1 artığı), anthropic yolunda "OpenAI çeviri" log etiketi (2084), salt-yazılır `openai_responder.enabled`.

## Test ve doğrulama sonuçları

- `py_compile` + `pyflakes` temiz; `node --check` + `scripts/check-inline-scripts.js` temiz.
- `python test_smoke.py` → `TUM TESTLER GECTI` (bu oturumda yeniden koşuldu; `test_verified_report_regressions` dahil).
- `tests/test_answer_harness.py` 6/6, `test_pronunciation_fixture.py` 9/9, çeviri işçisi 4/4, ses tanı 5/5, enhancement 16/16; frontend/cockpit/game-mode/overlay süitleri geçti; Electron testleri Electron runtime ile geçti (düz `node` ile açılışta Electron API'si yokluğundan düşer — beklenen).
- `tests/test_soak.py` 30 sn sentetik: 25 final / 445 partial / 0 hata / backlog 0 / thread sızıntısı yok (RSS bu ortamda `-1`).
- `npm run scan` temiz; `npm run build` → `dist/Whisper-Pro-win32-x64/Whisper-Pro.exe` (~190 MB) üretildi, paket-içi sızıntı taraması temiz, `PYTHON-GEREKSINIMI.txt` yerinde.
- `test_cevap_onerisi.py` çalıştırılmadı (ücretli API). `test_live_audio.py` fixture uyumsuzluğu raporlandı (yukarıda).

## Bilinen sınırlar / sonraki adımlar

- Bu makinede ses donanımı yok → WASAPI/mikrofon/oyun penceresi yolları Windows'ta elle doğrulanmalı.
- Önerilen uygulama sırası rapor §4'te: önce W-1 (backend koşulu + frontend alan düşürme + `apiKey:''` regresyon testi), sonra W-4 fixture hizalaması, kozmetikler.
- `dist/` bu kaynakla senkron; kod değişmediği için davranış farkı beklenmez.
