# Handoff - Public repository profile

## Identity

- Local repository: `D:\Whisper Live`
- Branch: `main`
- Public origin: `https://github.com/dorukakindev/whisper-live-translator.git`
- Starting code commit: `d5cfdb26ed3c22f677e7c343d5fc6f9c80c0c0d9`
- Completed public-profile commit: `41a2b462c46a494529a36d52d820215425d010e8`
- The public remote `main` matched the starting local commit before this work.

## Changes

- Updated the living README, installation guide, and fresh-machine guide to clone the renamed public repository rather than the former `whisper-live` URL.
- Added English Windows, MIT license, and public-checks badges to the README. Existing real-UI synthetic screenshots and demo video remain in place.
- Added repository, issues, homepage, keywords, and a more precise product description to `package.json`; no dependency versions changed.
- Added a read-only-permissions GitHub Actions workflow for Windows that installs locked Node dependencies, runs the current-source safety scan, and syntax-checks public utility scripts.
- Corrected `PUBLIC_RELEASE_CHECKLIST.md` to say the repository is already public and that historical credential candidates remain an active blocker. Historical handoff notes retain their original repository URLs as evidence of the earlier state.
- Pointed local `origin` to the new public repository. This Git remote configuration is local metadata, not a tracked file.

## Security finding

- On 2026-09-16, `npm run audit:history` returned exit code 1 after checking 66 reachable revisions. It reported nine redacted path/fingerprint combinations: three OpenAI-compatible candidates, one Google candidate, and one Hugging Face candidate repeated in five archived backup files.
- The current-source scan passed, but it cannot clear old Git blobs. No credential value was printed in this note or changed files.
- The owner must revoke or verify the candidates at their providers immediately. A history rewrite, force push, and coordination with existing clones or forks require a separate explicit decision. GitHub's documented sensitive-data-removal process may also require Support to clear cached views after a rewrite.
- No history rewrite, force push, repository visibility change, or credential rotation was performed in this work.

## Validation

Passed:

- Public GitHub API read confirmed repository visibility is public and the default branch is `main`.
- Public remote `main` and local HEAD matched before edits.
- `node --check scripts/audit-git-history.js`
- `node --check scripts/generate-public-demo.js`
- `npm ci --ignore-scripts --no-audit --no-fund`
- `npm run scan`
- `git diff --check`
- JSON parse and repository URL verification for `package.json`

Expected failure:

- `npm run audit:history` -> nine redacted historical candidate locations.

Not run:

- Python backend suites, live microphone/system-audio, external providers, clean-machine install, packaged EXE build, and hosted GitHub Actions run.

## Remaining work

- Check the hosted GitHub Actions run after push and repair any hosted-only failure.
- Set the public repository's English About description and relevant topic tags if API authentication is available.
- Investigate the historical candidate credentials with the owner; revoke/rotate any real credential, then plan a separately authorized, coordinated history cleanup.
- Review public archive notes and third-party model/dependency licenses before a formal binary release.

At note creation, this note's own documentation commit and push were pending. Confirm the remote SHA before calling this work published.
