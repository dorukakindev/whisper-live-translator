# Devir notu — üçüncü derin denetim turu (C-serisi bulgular)

## Kimlik

- Repo: `D:\Whisper Live`
- Dal: `main`
- Origin: `https://github.com/dorukakindev/whisper-live.git`
- Başlangıç kod commit'i: `0773407`
- Bitiş kod commit'i: `0773407` (uygulama kodu değişmedi; bu çalışma yalnız denetim + dokümantasyon)

## Yapılan iş

- Kullanıcının isteği üzerine denetim, önceki turlarda derinlemesine bakılmayan alanlara taşındı; uygulama koduna dokunulmadı.
- Kapsanan alanlar: `static/*.js` modüllerinin tamamı (cockpit, live-flow, reading-mode, runtime-safety, html-utils, quick-phrases), `audio_diagnostics.py`, `test_smoke.py` kapsamı, Socket.IO bağlantı güvenliği, ayar-kalıcılık yarışları (`persistRuntimeSettings`, `deepl_config`, `translation_settings`), `main.js` (restart/overlay/tray/global-PTT), mic executor slotları + `process_mic_audio`, `transkribe.py` (cihaz tespiti dahil), frontend backend-restart dalı.
- `GENEL_DENETIM_RAPORU_2026-09-15.md` güncellendi: 6 yeni bulgu (C-01..C-06), özet tablosu, öncelik sırası ve doğrulama tablosu yenilendi; geliştirme önerileri G-15/G-16 ile 16'ya çıktı.

## Bulgular (özet)

- **C-01 🟠 (canlı üretildi):** `POST /api/deepl_config` başarısız anahtar testine rağmen `translator.provider` ve `translator.api_key`'i mutasyona uğratıyor (`buyedektir.py:4687-4706` — provider koşulsuz yazılıyor, anahtar testten önce commit ediliyor). Flask test client ile üretildi: geçersiz anahtar gönderildi → `success:false` ama `provider='deepl'`, geçersiz `api_key` kayıtlı, `enabled=True` kaldı → sonraki her transkriptte sessiz çeviri hatası.
- **C-02 🟠 (canlı üretildi):** Socket.IO handshake'de token doğrulaması yok — `before_request` yalnız `/api/` prefix'ini koruyor (`buyedektir.py:119-128`); backend'de hiç `@socketio.on` handler yok. `socketio.test_client(app)` tokensuz → `is_connected() == True`. Canlı transkript/çeviri/konuşmacı akışı tokensuz okunabilir (Origin kontrolü tarayıcı-dışı süreçleri durdurmaz).
- **C-03 🟡:** `capture_mode='mic'` `whisper_language`'ı override etmiyor (`buyedektir.py:3457-3460`) — karşı taraf için 'ja' seçiliyken Türkçe dikte `language='ja'` ile çözümlenir → anlamsız çıktı. Alt-PTT `language='tr'` zorluyor (4969); davranış tutarsız.
- **C-04 🟡:** Ctrl-PTT (`setPtt`, `index.html:5037-5043`) ve Global-PTT (`main.js:438-484`) istek sıralaması yok — hızlı bas-bırakta sıralanan POST'lar `ptt_active`'i takılı bırakabilir; Alt-PTT'nin komut zinciri (`altPttCommandChain`) bu ikisinde yok.
- **C-05 🔵:** `backendRestarted` dalı `window._transcriptRevisions`'ı temizlemiyor (`index.html:3619-3629`) — backend restart'ta id'ler 0'dan başlayınca yeni kayıt eski yüksek revision'ı devralır ve `transcription_corrected` (rev=1) sessizce düşürülür.
- **C-06 🔵:** `transkribe.py` `torch.cuda.is_available()` kullanıyor — ana uygulamadaki gerçek-tensor CUDA testi (cuBLAS adı değişiminden doğan düzeltme) burada yok; GPU yüklemesi patlarsa CPU fallback de yok.
- Doğrulanıp bulgu OLMAYAN alanlar: `persistRuntimeSettings` aynı-path serileştirmesi doğru; kilit sırası (translator→responder) tutarlı; overlay'de generation/id/revision kontrolleri eksiksiz; `test_smoke.py` ~30 test içeriyor.

## Doğrulamalar

- `python -m py_compile buyedektir.py` ✅, `python -m pyflakes buyedektir.py` ✅, `python test_smoke.py` → `TUM TESTLER GECTI` ✅ (tekrar koşuldu).
- Flask test client ile C-01 üretildi (geçersiz DeepL anahtarı → runtime mutasyonu gözlendi).
- `socketio.test_client(app)` ile C-02 üretildi (tokensuz bağlantı kabul edildi).
- Tüm statik JS `node --check` ✅ (önceki turdan geçerli).
- Çalıştırılmayan: gerçek ses uçtan-uca testi, gerçek OpenAI/DeepL çağrısı (C-01 testi tek reddedilen istekle yapıldı), C-03/C-04/C-05 runtime üretimi (statik kanıt), diarization canlı gözlemi (B-01 nedeniyle imkânsız), `npm run build`.

## Bilinen sınırlar / sonraki adımlar

- Toplam açık bulgu 20 oldu (5 eski A + 9 B + 6 C); öncelik raporda güncellendi: B-01/B-02 → A-01 → A-02 → C-01 → B-03/B-04 → kalanlar.
- C-01'in düzeltmesi küçük: route test önce yapmalı, başarılıysa commit. C-02 için `io({auth:{token}})` + `connect` handler yeterli.
- `_quick_phrases.json` çalışma ağacında untracked (bu oturumun dosyası değil; sahnelenmedi).
- Kullanıcı düzeltme istemedi; uygulama koduna dokunulmadı.
