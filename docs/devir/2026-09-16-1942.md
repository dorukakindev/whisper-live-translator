# Handoff - English feature walkthrough and Game Mode media

## Identity

- Local repository: `D:\Whisper Live`
- Branch: `main`
- Public origin: `https://github.com/dorukakindev/whisper-live-translator.git`
- Starting code commit: `c94452bf7bffe698573254c9ec1443b41c982718`
- Ending code commit: `9cf02f241a3def0d2b3ec408913e4f1d8310b5a4`

## Changes

- Expanded `README.md` with an English feature table and practical workflows for live calls, selected-line translation, spoken replies, and Game Mode.
- Replaced the short usage page with a complete English guide covering setup, live partial/final text, automatic versus manual translation, reply suggestions, Reading mode, quick phrases, saved replies, self-authored responses, speaker diarization, conversation search, summaries, questions, export, reset, and troubleshooting.
- Documented Game Mode precisely: it opens an always-on-top overlay, applies a temporary system-audio dialogue profile, requires the normal listening flow, supports move/resize/appearance controls, and uses `Ctrl+Shift+O` and `Ctrl+Shift+L`.
- Documented the real limitations: Game Mode is not OCR or game injection, does not alter the selected model or microphone/PTT settings, and may be blocked by exclusive fullscreen or anti-cheat software.
- Added two English Game Mode screenshots rendered from the real Electron overlay with offline synthetic dialogue.
- Added `scripts/generate-public-game-demo.js` and `npm run demo:game-media` so both screenshots can be reproduced without a backend, microphone, API key, or private transcript.
- Removed a stray duplicated `npm start` line from the README quick-start section.

## Media

- `docs/media/whisper-pro-game-mode.png`: translated-dialogue overlay.
- `docs/media/whisper-pro-game-mode-settings.png`: the same overlay with Appearance controls open.
- Both PNG files are 1119x575. The renderer adds a visible `SYNTHETIC DEMO` label and uses no user data.

## Validation

Passed:

- `npm ci --no-audit --no-fund`
- `node --check scripts/generate-public-game-demo.js`
- `node --check scripts/generate-public-demo.js`
- `npm run demo:game-media`
- Game demo geometry assertion: viewport content height 383 px, settings bottom 338 px, no scroll overflow or clipping
- PNG parse and dimension check: both files 1119x575
- `npm run scan`
- Local Markdown target check for `README.md` and `docs/USAGE.md`
- `package.json` JSON parse
- `git diff --check`

Not run:

- Python backend tests, real microphone/system-audio capture, provider calls, game/anti-cheat testing, and portable EXE build. This work changes public documentation and an offline screenshot generator, not application runtime behavior.

## Dependencies and settings

- `npm ci` restored the project-local packages from the existing lockfile because the previous Electron installation was incomplete.
- No dependency version, backend setting, credential, schema, or user-data format changed.
- Generated temporary fixtures remain under ignored `artifacts/` paths.

## Remaining work

- Hosted GitHub Actions must pass after the two commits are pushed.
- A future release may add more English real-UI walkthrough images for settings, speaker diarization, and transcript export, using the same synthetic-data rule.

At note creation, this note's documentation commit and push were pending. Confirm the remote SHA before calling this work published.
