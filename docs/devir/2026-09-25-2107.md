# Devir Notu — 2026-09-25 21:07 (Europe/Istanbul)

## Kapsam ve Git durumu

- Repo: `dorukakindev/whisper-live-translator`; dal: `main`; origin: `https://github.com/dorukakindev/whisper-live-translator.git`.
- Başlangıç kod commit'i: `eacfcdb9d9891a330d2a710f350a18e4598cc0ce`.
- Bitiş kod commit'i: **aynı** — bu turda uygulama kodu değiştirilmedi (kullanıcı talimatı: "sen kodu değiştirme").
- Değişen dosyalar (yalnız belge): `docs/github-arastirma-2026-09-25.md` (yeni), bu devir notu, `DEVIR-NOTU.md` indeks güncellemesi.
- Çalışma ağacı başlangıçta temizdi (yalnız izlenmeyen `nul` dosyası vardı; dokunulmadı).

## Ne yapıldı

- Kullanıcı talebi: GitHub depolarından projeye uygulanabilir geliştirme/özellik fırsatlarının **kaynak kodu seviyesinde** araştırılıp raporlanması.
- GitHub MCP sunucusu `search_repositories` çağrılarında sürekli bağlantı hatası verdi (`get_me` çalışıyordu); aramalar `api.github.com` REST API + `curl` ile, kaynak dosyalar `raw.githubusercontent.com` ile okundu. Depo klonlanmadı.
- 14 depo incelendi; aralarında WhisperLiveKit (11k★), ufal SimulStreaming, LiveTranslate (700★), Sharrnah/whispering, free-cluely, diart, silero-vad, ten-vad, edge-tts, nllw/NLLB, romanizasyon kütüphaneleri (pykakasi, cutlet, python-pinyin, korean-romanizer, epitran, uroman), moonshine, argos-translate var.
- Kod okunan dosyalar: `whisperlivekit/local_agreement/{online_asr,whisper_online}.py`, `audio_processor.py`, `silero_vad_iterator.py`, `diarization/diart_backend.py`, `translation.py`; `LiveTranslate/vad_processor.py`, `asr_engine.py`, `translator.py`; `whispering/Models/STS/AudioEnhancer.py`; `free-cluely/electron/WindowHelper.ts`, `main.ts`.
- Bulgular `docs/github-arastirma-2026-09-25.md` içinde öncelik tablosuyla belgelendi. En yüksek etki/efor oranlı maddeler:
  1. Overlay'e `setContentProtection(true)` (free-cluely `WindowHelper.ts:102`) — ekran paylaşımında AI önerilerinin karşı tarafa görünmesini engeller; 1-2 satır.
  2. LocalAgreement-2 kararlı kısmi metin (WLK `online_asr.py` `HypothesisBuffer`: önek-anlaşmalı commit + 1–5 ngram dedup + init_prompt + anti-freeze) — canlı önizleme titremesini bitirir.
  3. NLLB-200/ctranslate2 yerel çeviri sağlayıcısı (WLK `nllw` yolu) — ücretsiz/çevrimdışı, faster-whisper'la aynı çekirdek.
  4. Capture-side Silero VAD — `faster_whisper.vad.get_speech_timestamps` kurulu sürümde mevcut (1.2.1 doğrulandı), sıfır ek bağımlılık.
  5. edge-tts ile okunuşun doğal sesle seslendirilmesi (şu an Windows `speechSynthesis`'e bağımlı).
  6. Progressif sessizlik katmanı (LiveTranslate `_progressive_tiers`): uzun tamponda daha kısa sessizlikle erken kesme — bizim `adaptive_silence_seconds` tek yönlü uzatıyor, kısaltma yönü eksik.
  7. Canonical romanizasyon doğrulaması (pypinyin/pykakasi/korean-romanizer) — LLM okunuş halüsinasyonunu yakalamak için.
- Yerel doğrulamalar: faster-whisper 1.2.1'de `vad.py` (Silero) + `BatchedInferencePipeline` + `large-v3-turbo`/`turbo` mevcut; UI'da turbo seçeneği zaten var. `OPENAI_BASE_URL`'ün reseller dışına bilinçli kapalı olduğu (buyedektir.py:1367-1426) Groq/yerel LLM önerilerinde not edildi.

## Test ve doğrulama sonuçları

- Kod değişmediği için test koşulmadı; yalnız belge commit'i.
- Çalıştırılmayan kontroller: test_smoke, pyflakes vb. — gerek yok (kod diff'i yok).

## Bilinen sınırlar / sonraki adımlar

- Rapor §"Öncelik sırası" uygulama sırası önerisidir; hiçbiri kodlanmadı.
- diart'ın `numpy<2` + Python ≤3.12 kısıtı ve moonshine'ın non-EN ticari lisansı uygulamadan önce doğrulanmalı.
- Bir sonraki turda uygulanacaksa 1 (contentProtection) ve 4 (silero-vad geçişi) en düşük riskli başlangıç.
