# Devir notu — ikinci derin denetim turu ve rapor güncellemesi

## Kimlik

- Repo: `D:\Whisper Live`
- Dal: `main`
- Origin: `https://github.com/dorukakindev/whisper-live.git`
- Kod commit'i: `8468520` (uygulama kodu değişmedi; bu çalışma yalnız denetim + dokümantasyon)

## Yapılan iş

- Kullanıcının isteği üzerine denetim derinleştirilerek devam etti; uygulama koduna dokunulmadı.
- Kapsanan alanlar: backend'in tamamı (capture/VAD/PTT/commit/çeviri worker'ı/mic yolu/AI yanıt route'u/diarization/yaşam döngüsü), Electron ana süreç + preload, overlay, `transkribe.py`, `build.js`, `launcher_whisper.py`, `başlat.bat`, `requirements.txt` ve **kurulu `.venv` paket sürümleri** (pyannote.audio 4.0.4, torch 2.10.0+cu130, torchcodec).
- `GENEL_DENETIM_RAPORU_2026-09-15.md` yeniden yazıldı: 10 yeni bulgu (B-01..B-10 → son taslakta B-01..B-09 + demet maddesi), iki ilk-tur bulgusunun düzeltmesi (A-06, A-07 gerçekte düzeltilmişti), güncellenmiş öncelik sırası ve doğrulama tablosu.

## Bulgular (özet)

- Yeni kritik: **B-01** — konuşmacı tanıma pyannote.audio 4.x'te sessizce ölü (`DiarizeOutput.itertracks` yok; `requirements.txt` üst sınırsız `>=2.1.1`, kurulu 4.0.4; `.venv`'de `hasattr` ile doğrulandı). **B-02** — aynı ortamda torchcodec bozuk (`libtorchcodec_core8.dll` yüklenemiyor) → dosya-yolu fallback'i de ölü.
- Yeni orta/düşük: **B-03** openai çeviri sağlayıcısında anahtarsız "aktif" mesajı + ana cevap anahtarına fallback yok; **B-04** `configure_translation(provider, None)` kayıtlı anahtarı siliyor; **B-05** Alt-PTT sonucu generation-uyuşmazlığında emit'siz düşüyor → UI "işleniyor"da takılı; **B-06** `context_buffer` oturumlar arası temizlenmiyor + mic-dikte (TR) Whisper prompt'unu kirletiyor; **B-07** int olmayan `transcript_id` bağlamı kendini içeriyor; **B-08** `target_lang` doğrulanmıyor; **B-09** küçük sağlamlık demeti (bool-coercion, PyAudio leak, `ai_available` tutarsızlığı, mojibake banner, ölü DEFAULTS anahtarları).
- İlk tur düzeltmeleri: **A-06** (mic `conversation_turns.append` aslında `_lifecycle_lock` içinde, 5097-5120) ve **A-07** (CSP `after_request` başlığı var, 104-116) iptal edildi → ✅.
- Hâlâ açık: A-01 (Durdur'da son konuşma), A-02 (yol eşleşmesi; göreli-yol varyantı da eklendi), A-03 (çift çeviri), A-04 (sessiz CPU), A-05 (Ctrl-PTT keepalive).
- Geliştirme önerileri G-01..G-14'e genişletildi.

## Doğrulamalar

- pyannote.audio 4.0.4 API'si doğrudan incelendi: `SpeakerDiarization(legacy=False)` → `DiarizeOutput`, `itertracks` metodu yok → B-01 kanıtı.
- `process_mic_audio` kilit aralıkları, `context_buffer` kaynak akışı, `configure_translation` anahtar yazımı, `getAIResponseById`/`transcript_id` gönderimi satır numarasıyla doğrulandı.
- Önceki doğrulamalar geçerli: `py_compile`, `pyflakes`, `test_smoke.py` (`TUM TESTLER GECTI`), `node --check` (tüm statik JS + literal-substituted inline scriptler).

## Bilinen sınırlar / sonraki adımlar

- Gerçek ses akışıyla uçtan uca test ve gerçek OpenAI/DeepL çağrıları yapılmadı; B-01/B-02 diarization açık bir oturumda yeniden üretilmeli.
- Öncelik önerisi raporda: B-01+B-02 → A-01 → A-02 → B-03/B-04 → A-04/A-03 → B-05 → B-06 → kalanlar.
- `_quick_phrases.json` çalışma ağacında untracked (bu oturumun dosyası değil; sahnelenmedi).
- Kullanıcı düzeltme istemedi; uygulama koduna dokunulmadı.
