flask>=2.0.0
flask-socketio>=5.0.0
faster-whisper>=0.9.0
pyaudiowpatch>=0.2.12.3
numpy>=1.21.0
webrtcvad-wheels==2.0.14
requests>=2.28.0
pyannote.audio>=2.1.1
# torch/torchaudio: `pip install -r requirements.txt` bu kaliplarla PyPI'dan
# CPU-only wheel kurar (GPU'suz makinelerde calismasi icin varsayilan budur).
# NVIDIA GPU'nuz varsa GPU hizlandirmasi icin (buyuk hiz/kalite farki) ONCE
# bunlari kaldirip su sekilde CUDA wheel'i kurun (surucunuzun desteklediginin
# esit veya altindaki en yeni cuXXX index'i secin, orn. surucu CUDA 13.x
# destekliyorsa cu130):
#   pip install --index-url https://download.pytorch.org/whl/cu130 torch torchaudio
# load_model gercek bir torch.cuda islemiyle GPU'yu otomatik algilar; ayarda
# elle bir sey degistirmeniz gerekmez.
torch>=2.0.0
torchaudio>=2.0.0
scipy>=1.9.0
openai>=1.0.0
python-dotenv>=0.20.0
